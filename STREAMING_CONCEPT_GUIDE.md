# Gaussian Splat 애니메이션 스트리밍 시스템 - 개념 가이드

## 📚 목차

1. [스트리밍이란 무엇인가?](#스트리밍이란-무엇인가)
2. [Unity Addressables란?](#unity-addressables란)
3. [우리가 구현한 시스템](#우리가-구현한-시스템)
4. [기술적 동작 원리](#기술적-동작-원리)
5. [성능 및 메모리 비교](#성능-및-메모리-비교)

---

## 스트리밍이란 무엇인가?

### 기본 개념

**스트리밍 (Streaming)**은 **필요한 데이터만 실시간으로 로드하면서 재생하는 방식**입니다.

### 비유로 이해하기

#### 🎬 영화 시청

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **다운로드 후 재생** | 영화 전체(5GB)를 다운로드 후 재생 | 재생 중 끊김 없음 | 대기 시간 김, 저장 공간 필요 |
| **스트리밍 (Netflix)** | 필요한 부분만 버퍼링하며 재생 | 즉시 시작, 저장 공간 불필요 | 네트워크 필요 |

#### 🎮 게임에서의 스트리밍

```
전통적 방식:
├─ 시작 시: 모든 레벨, 텍스처, 모델 로드 (10GB)
├─ 문제: 로딩 시간 길고, 메모리 부족
└─ 재생: 빠르지만 모든 것이 메모리에

스트리밍 방식:
├─ 시작 시: 현재 레벨만 로드 (500MB)
├─ 플레이 중: 다음 영역 미리 로드 (백그라운드)
├─ 이전 영역: 메모리에서 제거
└─ 결과: 빠른 시작, 낮은 메모리, 무한 콘텐츠 가능
```

### 우리 프로젝트에 적용

#### 문제 상황
```
Gaussian Splat 애니메이션: 300프레임
각 프레임 크기: 약 17MB
전체 크기: 300 × 17MB = 5.1GB
```

**전통적 방식:**
```cpp
// 시작 시
for (int i = 0; i < 300; i++) {
    frames[i] = LoadFrame(i);  // 5GB 메모리 사용!
}
// 문제: 메모리 부족, 로딩 시간 5분+
```

**스트리밍 방식:**
```cpp
// 현재 프레임만 로드
currentFrame = LoadFrame(0);        // 17MB

// 재생 중 다음 프레임 준비
while (playing) {
    PrefetchFrame(currentIndex + 1); // 백그라운드 로드
    DisplayFrame(currentFrame);       // 즉시 표시
    ReleaseFrame(currentIndex - 5);   // 오래된 프레임 제거
}
// 결과: 메모리 ~100MB, 즉시 시작
```

---

## Unity Addressables란?

### 개념

**Addressables**는 Unity의 **에셋 관리 및 로딩 시스템**으로, **주소(Address)로 에셋을 참조하고 동적으로 로드**할 수 있게 해줍니다.

### 기존 방식 vs Addressables

#### ❌ 기존 방식: Resources

```csharp
// 1. 에셋을 Resources/ 폴더에 넣어야 함
// 2. 빌드에 모두 포함됨 (빌드 크기 증가)
// 3. 로컬만 가능, 원격 로딩 불가능

GaussianSplatAsset frame = Resources.Load<GaussianSplatAsset>("Frames/frame_0001");
```

**문제점:**
- ✅ 간단함
- ❌ 빌드 크기 5GB+
- ❌ 원격 서버에서 로드 불가능
- ❌ 메모리 관리 수동
- ❌ 업데이트 시 전체 재배포 필요

#### ✅ Addressables

```csharp
// 1. 에셋을 어디든 둘 수 있음
// 2. 빌드에서 분리 가능 (빌드 < 100MB)
// 3. 로컬/원격 모두 가능
// 4. 자동 메모리 관리 (Reference Counting)

var handle = Addressables.LoadAssetAsync<GaussianSplatAsset>("gaussian_frames/frame_0001");
await handle.Task;
GaussianSplatAsset frame = handle.Result;

// 사용 후 해제
Addressables.Release(handle); // 메모리 자동 정리!
```

**장점:**
- ✅ 빌드에서 분리 → 작은 빌드 크기
- ✅ HTTP 서버에서 로드 가능 (CDN 사용 가능)
- ✅ 자동 메모리 관리
- ✅ 콘텐츠 업데이트 시 일부만 교체 가능
- ✅ 플랫폼별 최적화 (압축, 번들링)

### Addressables의 핵심 기능

#### 1. 주소 기반 참조
```csharp
// 파일 경로가 아닌 "주소"로 참조
"gaussian_frames/frame_0001"  // 주소
"gaussian_frames/frame_0234"
```

#### 2. 번들링 (Bundling)
```
에셋들을 압축된 번들 파일로 패키징
├─ frame_0001 → bundle_abc123.bundle (압축)
├─ frame_0002 → bundle_def456.bundle
└─ ...
```

**Pack Separately vs Pack Together:**
```
Pack Together (기본값):
└─ all_frames.bundle (5GB) ← 전부 하나로!
    ├─ frame_0001
    ├─ frame_0002
    └─ ... (300개)
문제: 하나 로드하려면 5GB 전체 다운로드

Pack Separately (스트리밍용):
├─ frame_0001.bundle (17MB)
├─ frame_0002.bundle (17MB)
└─ ... (300개 개별 번들)
장점: 필요한 것만 로드!
```

#### 3. 원격 로딩
```
로컬 (에디터/빌드):
file:///C:/Project/ServerData/frame_0001.bundle

원격 (서버):
http://localhost:8000/frame_0001.bundle

CDN (프로덕션):
https://cdn.example.com/bundles/frame_0001.bundle
```

#### 4. 자동 의존성 관리
```csharp
// Handle 기반 참조 카운팅
AsyncOperationHandle<T> handle = Addressables.LoadAssetAsync<T>(key);
// 내부적으로 참조 카운트 +1

Addressables.Release(handle);
// 참조 카운트 -1, 0이 되면 메모리 자동 해제
```

---

## 우리가 구현한 시스템

### 전체 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                  Unity Scene                             │
│  ┌────────────────────────────────────────────────┐     │
│  │ AddressablesStreamingPlayer (Component)         │     │
│  │                                                  │     │
│  │ ┌─────────────┐  ┌──────────────┐              │     │
│  │ │ Prefetch    │  │ Playback     │              │     │
│  │ │ Loop        │  │ Loop         │              │     │
│  │ │ (백그라운드) │  │ (메인 스레드) │              │     │
│  │ └─────────────┘  └──────────────┘              │     │
│  │         │               │                        │     │
│  │         └───────┬───────┘                        │     │
│  │                 ▼                                 │     │
│  │    ┌────────────────────────┐                   │     │
│  │    │ Addressables System     │                   │     │
│  │    └────────────────────────┘                   │     │
│  └────────────────────┬───────────────────────────┘     │
└───────────────────────┼─────────────────────────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │   Addressables Bundles         │
        │                                 │
        │  ┌──────┐ ┌──────┐ ┌──────┐  │
        │  │Bundle│ │Bundle│ │Bundle│  │
        │  │ 0001 │ │ 0002 │ │ 0003 │  │
        │  └──────┘ └──────┘ └──────┘  │
        │       ... (300개) ...         │
        └───────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
    로컬 파일        HTTP 서버        CDN
```

### 주요 컴포넌트

#### 1. AddressablesStreamingPlayer.cs

**역할:** Gaussian Splat 애니메이션 스트리밍 제어

**핵심 기능:**
```csharp
public class AddressablesStreamingPlayer : MonoBehaviour
{
    // 설정
    public GaussianSplatRenderer targetRenderer;
    public string addressKeyPattern = "gaussian_frames/frame_{0:D4}";
    public int prefetchCount = 5;  // 미리 로드할 프레임 개수
    
    // 메모리 관리
    private Dictionary<int, GaussianSplatAsset> loadedAssets;      // 로드된 에셋
    private Dictionary<int, AsyncOperationHandle> assetHandles;    // Handle 추적
    
    // 코루틴
    private IEnumerator PrefetchLoop()    // 백그라운드 로딩
    private IEnumerator PlaybackLoop()    // 재생 루프
    private void CleanupOldFrames()       // 메모리 정리
}
```

#### 2. AddressablesSetupHelper.cs (Unity Editor)

**역할:** Gaussian 에셋을 Addressables로 자동 변환

**동작:**
1. `Assets/GaussianAssets/` 스캔
2. 파일명에서 프레임 번호 추출 (`frames-0242` → `0242`)
3. Addressables 주소 생성 (`gaussian_frames/frame_0242`)
4. Pack Separately 모드로 그룹 생성

#### 3. addressables_server.py

**역할:** 로컬/네트워크 HTTP 서버

**동작:**
```python
# ServerData/ 폴더를 HTTP로 서빙
# http://localhost:8000/StandaloneWindows64/bundle_xxx.bundle
```

**용도:**
- 로컬 테스트
- 네트워크 스트리밍 (같은 WiFi의 다른 디바이스)
- CDN 배포 전 검증

---

## 기술적 동작 원리

### 1. 초기화 (Start)

```csharp
void Start()
{
    loadedAssets = new Dictionary<int, GaussianSplatAsset>();
    assetHandles = new Dictionary<int, AsyncOperationHandle>();
    
    if (playOnStart)
        Play();
}
```

### 2. 재생 루프 (PlaybackLoop)

```csharp
private IEnumerator PlaybackLoop()
{
    while (isPlaying)
    {
        // 1. 현재 프레임이 로드되었는지 확인
        if (loadedAssets.ContainsKey(currentFrame))
        {
            // 2. 즉시 표시!
            targetRenderer.asset = loadedAssets[currentFrame];
            
            // 3. 다음 프레임으로 진행
            currentFrame++;
        }
        else
        {
            // 4. 아직 로드 안됨 → 대기
            yield return null;
        }
        
        // 5. FPS에 맞춰 대기
        yield return new WaitForSeconds(1f / fps);
    }
}
```

### 3. Prefetch 루프 (백그라운드 로딩)

```csharp
private IEnumerator PrefetchLoop()
{
    while (isPlaying)
    {
        // 현재 + 다음 5프레임을 미리 로드
        for (int i = 0; i < prefetchCount; i++)
        {
            int frameToLoad = currentFrame + i;
            
            // 이미 로드된 프레임은 스킵
            if (!loadedAssets.ContainsKey(frameToLoad))
            {
                StartCoroutine(LoadFrameAsync(frameToLoad));
            }
        }
        
        yield return new WaitForSeconds(0.1f); // 100ms마다 체크
    }
}
```

### 4. 비동기 로딩 (LoadFrameAsync)

```csharp
private IEnumerator LoadFrameAsync(int frameNumber)
{
    // 주소 생성: "gaussian_frames/frame_0042"
    string addressKey = string.Format(addressKeyPattern, frameNumber);
    
    // Addressables 로드 시작
    var handle = Addressables.LoadAssetAsync<GaussianSplatAsset>(addressKey);
    
    // 로딩 완료 대기 (비동기!)
    yield return handle;
    
    // 성공 시 저장
    if (handle.Status == AsyncOperationStatus.Succeeded)
    {
        loadedAssets[frameNumber] = handle.Result;
        assetHandles[frameNumber] = handle;  // Handle 추적 (메모리 관리용)
    }
}
```

### 5. 메모리 정리 (CleanupOldFrames)

```csharp
private void CleanupOldFrames()
{
    List<int> framesToRemove = new List<int>();
    
    // 현재 프레임 ± 5 범위를 벗어난 프레임 찾기
    foreach (int frame in loadedAssets.Keys)
    {
        if (frame < currentFrame - 5 || frame > currentFrame + prefetchCount + 5)
        {
            framesToRemove.Add(frame);
        }
    }
    
    // 메모리에서 제거
    foreach (int frame in framesToRemove)
    {
        Addressables.Release(assetHandles[frame]); // ← 핵심! 메모리 해제
        assetHandles.Remove(frame);
        loadedAssets.Remove(frame);
    }
}
```

### 타임라인 예시

```
시간   현재   메모리에 로드된 프레임           동작
────────────────────────────────────────────────────────
t=0    0      [0, 1, 2, 3, 4, 5]              Prefetch 0~5
t=1    1      [0, 1, 2, 3, 4, 5, 6]           표시:1, Prefetch:6
t=2    2      [0, 1, 2, 3, 4, 5, 6, 7]        표시:2, Prefetch:7
t=3    3      [1, 2, 3, 4, 5, 6, 7, 8]        표시:3, Cleanup:0, Prefetch:8
t=4    4      [2, 3, 4, 5, 6, 7, 8, 9]        표시:4, Cleanup:1, Prefetch:9
...
```

**메모리 사용:** 항상 8~10프레임만 유지 (약 150MB)

---

## 성능 및 메모리 비교

### 메모리 사용량

| 방식 | 메모리 | 설명 |
|------|--------|------|
| **모든 프레임 로드** | 5.1 GB | 300 × 17MB |
| **Streaming (Prefetch=2)** | 51 MB | 3프레임 × 17MB |
| **Streaming (Prefetch=5)** | 119 MB | 7프레임 × 17MB |
| **Streaming (Prefetch=10)** | 187 MB | 11프레임 × 17MB |

**절감률:** 97% ~ 99%

### 로딩 시간

| 방식 | 초기 로딩 | 재생 시작 |
|------|-----------|-----------|
| **전체 로드** | 2-5분 | 로딩 후 즉시 |
| **Streaming** | <1초 | 거의 즉시 |

### 디스크 I/O

| 방식 | 읽기 패턴 | 총 읽기량 (1회 재생) |
|------|-----------|---------------------|
| **전체 로드** | 시작 시 한 번 | 5.1GB |
| **Streaming** | 지속적 | 5.1GB (동일) |

**차이:** 로드 시점만 다름 (시작 vs 재생 중)

### 네트워크 대역폭 (원격 스트리밍)

```
FPS: 30
프레임당 크기: 17MB
필요 대역폭: 17MB × 30 = 510MB/s = 4Gbps

실제 필요 (Prefetch=5):
피크: ~100MB/s (burst)
평균: ~17MB/s (sustained)
```

**인터넷 스트리밍은 현재 대역폭으로는 어려움**
→ 로컬/LAN 환경 적합

### Prefetch Count별 성능

| Prefetch | 메모리 | 버퍼링 여유 | 권장 환경 |
|----------|--------|-------------|-----------|
| 1 | 34MB | 33ms | SSD, 짧은 애니메이션 |
| 2 | 51MB | 66ms | SSD |
| **5** | **119MB** | **166ms** | **HDD, 안정적** ✅ |
| 10 | 187MB | 333ms | 느린 스토리지 |

**최적값:** Prefetch=5 (안정성 vs 메모리 균형)

---

## 요약

### 우리가 해결한 문제

**Before:**
- ✅ 300프레임 애니메이션 작동
- ❌ 5GB 메모리 필요
- ❌ 2-5분 로딩
- ❌ 메모리 부족으로 확장 불가능

**After:**
- ✅ 300프레임 부드러운 재생
- ✅ 120MB 메모리 (97% 절감)
- ✅ <1초 시작
- ✅ 무한 확장 가능
- ✅ 원격 스트리밍 가능
- ✅ CDN 배포 가능

### 핵심 기술

1. **Unity Addressables**: 주소 기반 동적 로딩
2. **Pack Separately**: 개별 번들로 분리
3. **Prefetch**: 백그라운드 미리 로딩
4. **Memory Cleanup**: 자동 메모리 관리
5. **Async Loading**: 메인 스레드 블로킹 방지

### 스트리밍의 본질

**"필요한 데이터만, 필요한 시점에, 백그라운드에서 준비하여, 끊김 없이 제공"**

이것이 Netflix, YouTube, 게임 오픈월드 스트리밍의 핵심이며,
우리가 Gaussian Splat 애니메이션에 적용한 방식입니다.
